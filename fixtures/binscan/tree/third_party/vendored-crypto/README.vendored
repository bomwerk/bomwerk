Prebuilt vendored library: the repository ships libvendcrypto.so.1 and
libvendcrypto.a, never their sources. bomwerk never watches either get
compiled, so the binary pass is the only thing that can say what they are.

Both binaries are GENERATED by scripts/run_binscan_case.py at test time rather
than committed here — see ../../SOURCES.md for why.
