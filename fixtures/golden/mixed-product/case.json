{
  "description": "The seam: fmt is vendored as a submodule AND declared via FetchContent at the same commit, so two producers emit one purl and core::merge_all must yield a single component; --product/--product-version populate metadata.component",
  "args": ["--product", "golden-demo", "--product-version", "4.5.6"],
  "expect_exit": 0
}
