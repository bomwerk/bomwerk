The real .a lives in bomwerk/nightmare. trim only needs this directory to exist so the
link input's containing directory canonicalizes the same way the scan root does; the
archive itself is never read, and a fake binary here would be a fabricated fixture.
